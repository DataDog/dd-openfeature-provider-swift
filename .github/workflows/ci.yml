name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: read

jobs:
  test:
    name: Test
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
        swift-version: ['5.9', '5.10', '6.0']
    
    steps:
    - name: Checkout
      uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
    
    - name: Setup Swift
      uses: swift-actions/setup-swift@65540b95f51493d65f5e59e97dcef9629ddf11bf # v2.0.0
      with:
        swift-version: ${{ matrix.swift-version }}
    
    - name: Cache Swift Package Manager
      uses: actions/cache@6849a6489940f00c2f30c0fb92c6274307ccb58a # v3.3.2
      with:
        path: .build
        key: ${{ runner.os }}-spm-${{ hashFiles('**/Package.resolved') }}
        restore-keys: |
          ${{ runner.os }}-spm-
    
    - name: Build
      run: swift build -v
    
    - name: Run tests
      run: swift test -v
  
  ios-test:
    name: iOS Test
    runs-on: macos-latest
    
    steps:
    - name: Checkout
      uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
    
    - name: Select Xcode version
      run: sudo xcode-select -s /Applications/Xcode_15.0.app/Contents/Developer
    
    - name: Cache Swift Package Manager
      uses: actions/cache@6849a6489940f00c2f30c0fb92c6274307ccb58a # v3.3.2
      with:
        path: .build
        key: ${{ runner.os }}-spm-ios-${{ hashFiles('**/Package.resolved') }}
        restore-keys: |
          ${{ runner.os }}-spm-ios-
    
    - name: Build for iOS
      run: xcodebuild -scheme DataDogOpenFeatureProvider -destination "platform=iOS Simulator,name=iPhone 15,OS=latest" build
    
    - name: Test on iOS
      run: xcodebuild -scheme DataDogOpenFeatureProvider -destination "platform=iOS Simulator,name=iPhone 15,OS=latest" test
